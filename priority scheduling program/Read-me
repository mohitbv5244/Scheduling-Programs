This program executes few tasks
with the process time and the priority.

We use slection sort to sort the priority with their
respective procrss time.
Then we calculate the waiting time for each task and
turn around time.

The priority program executes the
tasks depending on their arrival order.
